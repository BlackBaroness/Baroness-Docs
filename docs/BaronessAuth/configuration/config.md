---
sidebar_position: 1
title: Актуальный конфиг
---

Здесь отображается актуальные файлы конфигурации плагина.

В них содержится большая часть документации, с редкими ссылками на этот сайт.

<details>  
<summary>config.yml</summary>

```yaml
# ┌─────────────────────────────────────────────────┐
# │ Некоторые настройки выделены в отдельные файлы: │
# │                                                 │
# │ |=> database.yml - настройки базы данных        │
# │ |=> antibot.yml - настройки антибота            │
# └─────────────────────────────────────────────────┘
#  
#  
#  
# ┌────────────────────────────────────────────────────────────────────────────────────┐
# │                        Настройки провайдеров аутентификации                        │
# │ Подробнее: https://docs.baronessdev.ru/BaronessAuth/configuration/providers/basics │
# └────────────────────────────────────────────────────────────────────────────────────┘
auth-providers:
  #  
  # Список включённых провайдеров.
  # При входе игрока BaronessAuth проверяет провайдеры по порядку,
  # в поисках того, который подойдёт (выбирается первый подходящий).
  enabled-providers:
    - 'classic'
  #  
  # Сообщение при попытке регистрации, когда регистрация не нужна.
  register-not-require: '§c§lВам не нужна регистрация.'
  #  
  # Сообщение при попытке авторизации, когда авторизация не нужна.
  login-not-require: '§c§lВам не нужна авторизация.'
  #  
  # Настройки обработки ситуации, когда входящему игроку не удаётся подобрать провайдер.
  no-providers:
    # Нужно ли кикать игрока?
    kick: true
    # Сообщение при кике.
    message: '§cВы не подходите под условия ни одного способа авторизации.'
  #  
  #  
  # ┌──────────────────────────────────────────────────┐
  # │             "Классический" провайдер             │
  # │                Название: classic                 │
  # │                                                  │
  # │ Классическая регистрация и авторизация по паролю.│
  # └──────────────────────────────────────────────────┘
  classic:
    # Нужно ли право для использования провайдера?
    use-permission: false
    # Право для использования провайдера.
    permission: 'baronessauth.provider.classic'
    #  
    # Настройки регистрации.
    register:
      # Сообщения.
      messages:
        # Сообщение о необходимости зарегистрироваться.
        required:
          # Переодически повторяемое сообщение в чате.
          message: 'Зарегистрируйтесь, используя §6/register [пароль] [повтор пароля]§r.'
          # Title Bar, единожды отсылается на весь период регистрации.
          # Установите оба значения на '', чтобы выключить.
          titlebar:
            title: '§6§lЗарегистрируйтесь:'
            subtitle: '§b§l/reg [пароль] [повтор пароля]§r'
        #  
        # Сообщение об успешной регистрации.
        completed:
          # Сообщение в чате.
          message: '§a§lВы успешно зарегистрировались!'
          # Title Bar, единожды отсылается на 2 секунды.
          # Установите оба значения на '', чтобы выключить.
          titlebar:
            title: '§a§lДобро пожаловать!'
            subtitle: ''
        #  
        # Сообщение о несовпадении паролей, если включен параметр `need-repeat-password`
        notSame: '§c§lПароли не совпадают.'
        # Сообщение при кике игрока, если он не зарегистрировался за указанное время
        kick: '§c§lВремя регистрации истекло.'
      #  
      # Определяет, нужно ли повторять пароль вторым аргументом
      need-repeat-password: true
      # Частота повторения `register.messages.required` в секундах
      repeat: 4
      # Максимальное время для регистрации в секундах
      timeout: 100
    #  
    # Настройки авторизации.
    login:
      # Сообщения.
      messages:
        # Сообщение о необходимости авторизоваться.
        required:
          # Переодически повторяемое сообщение в чате.
          message: 'Авторизуйтесь, используя §6/login [пароль]§r.'
          # Title Bar, единожды отсылается на весь период регистрации.
          # Установите оба значения на '', чтобы выключить.
          titlebar:
            title: '§6§lАвторизуйтесь:'
            subtitle: '§b§l/l [пароль]§r'
        #  
        # Сообщение об успешной авторизации.
        completed:
          # Сообщение в чате.
          message: '§a§lВы успешно авторизованы!'
          # Title Bar, единожды отсылается на 2 секунды.
          # Установите оба значения на '', чтобы выключить.
          titlebar:
            title: '§a§lПриятной игры!'
            subtitle: ''
        #  
        # Сообщение о неверном пароле.
        false: '§c§lНеправильный пароль.'
        # Сообщение при кике игрока, если он не авторизовался за указанное время.
        kick: '§c§lВремя авторизации истекло.'
      #  
      # Частота повторения `login.messages.required` в секундах.
      repeat: 4
      # Максимальное время для авторизации в секундах.
      timeout: 40
  #  
  #  
  # ┌────────────────────────────────────────────────────────────────────────────────┐
  # │                                 Бот ВКонтакте                                  │
  # │                                  Название: vk                                  │
  # │ Подробнее: https://docs.baronessdev.ru/BaronessAuth/configuration/providers/vk │
  # └────────────────────────────────────────────────────────────────────────────────┘
  vk:
    # Нужно ли право для использования провайдера.
    use-permission: false
    # Право для использования провайдера.
    permission: 'baronessauth.provider.vk'
    # Ключ доступа от сообщества.
    token: 'enter-your-vk-longpoll-token-here'
    # Числовой ID сообщества.
    id: 123
    hover:
      # Текст для `required` сообщений, который выводится при наведении.
      text: '§fНажмите, чтобы открыть бота по ссылке.'
      # Ссылка для `required` сообщений, которая открывается при клике.
      url: 'https://vk.com/bauth_example_bot'
    #  
    register:
      messages:
        # Переодически повторяемое сообщение о необходимости зарегистрироваться.
        # {CODE} меняется на код, который нужно отправить боту.
        required: 'Зарегистрируйтесь, написав §6{CODE}§r боту §6@mybot§r в VK.'
        # Сообщение об успешной регистрации.
        completed: '§a§lВы успешно зарегистрировались!'
        # Ответное сообщение при присылании неизвестного кода.
        vk-error: 'Неизвестный код. Попробуйте перезайти на сервер и получить новый.'
        # Сообщение об успешной регистрации (в диалоге), {PASS} меняется на пароль.
        vk-success:
          - 'Вы успешно зарегистрировались!'
          - 'Каждый вход будет требовать подтверждения в этом диалоге.'
          - ''
          - 'Для вас сгенерирован сложный пароль:'
          - '{PASS}'
          - ''
          - 'Вы можете использовать его в экстренных ситуациях.'
        # Сообщение при кике игрока, если он не зарегистрировался за указанное время.
        kick: '§c§lВремя регистрации истекло.'
      #  
      # Частота повторения `register.messages.required` в секундах.
      repeat: 4
      # Максимальное время для регистрации в секундах.
      timeout: 300
    #  
    login:
      messages:
        # Переодически повторяемое сообщение о необходимости авторизоваться.
        required: 'Авторизуйтесь, приняв запрос бота §6@mybot§r в VK.'
        # Сообщение об успешной авторизации.
        completed: '§a§lВы успешно авторизованы!'
        # Сообщение о неверном пароле.
        false: '§c§lНеправильный пароль.'
        # Сообщение при кике игрока, если он не авторизовался за указанное время.
        kick: '§c§lВремя авторизации истекло.'
        vk-request:
          # Сообщение владельцу аккаунта, {NICK} - ник, {IP} - IP адрес, откуда запрошен вход.
          message:
            - 'На ваш аккаунт ({NICK}) совершается попытка входа с IP-адреса {IP}.'
            - 'Если это вы, нажмите на :white_check_mark:, если нет - на :x:.'
            - 'Отклонение запроса мгновенно кикнет игрока с сервера.'
          # Текст кнопки "принять".
          button-1: ':white_check_mark:'
          # Текст кнопки "отклонить".
          button-2: ':x:'
          # Сообщение о принятии входа, {NICK} - ник, {IP} - IP адрес, откуда запрошен вход.
          accepted: ':white_check_mark: | Разрешён вход {NICK} с {IP}'
          # Сообщение об отклонении входа, {NICK} - ник, {IP} - IP адрес, откуда запрошен вход.
          rejected: ':x: | Отклонён вход {NICK} с {IP}'
          # Сообщение о том, что запрошенный игрок уже авторизован или вышел с сервера.
          offline: ':thinking:| Похоже, что игрок оффлайн или уже авторизован.'
          # Сообщение при кике игрока, если владелец аккаунта отклонил вход.
          kick: '§c§lВладелец аккаунта отклонил ваш вход.'
      #  
      # Частота повторения `login.messages.required` в секундах.
      repeat: 4
      # Максимальное время для авторизации в секундах.
      timeout: 40
      # Определяет, доступна ли команда /login.
      login-command-available: true
  #  
  #  
  # ┌──────────────────────────────────────────────────────────────────────────────────────┐
  # │                                    Бот Telegram                                      │
  # │                                 Название: telegram                                   │
  # │ Подробнее: https://docs.baronessdev.ru/BaronessAuth/configuration/providers/telegram │
  # ┌──────────────────────────────────────────────────────────────────────────────────────┘
  telegram:
    # Нужно ли право для использования провайдера?
    use-permission: false
    # Право для использования провайдера.
    permission: 'baronessauth.provider.telegram'
    # Токен от бота вида `110201543:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw`.
    token: 'enter-your-telegram-bot-token-here'
    # ID бота.
    id: 'enter-your-telegram-bot-id-here'
    hover:
      # Текст для `required` сообщений, который выводится при наведении.
      text: '§fНажмите, чтобы открыть бота по ссылке.'
      # Ссылка для `required` сообщений, которая открывается при клике.
      url: 'https://t.me/bauth_example_bot'
    #  
    register:
      messages:
        # Переодически повторяемое сообщение о необходимости зарегистрироваться.
        # {CODE} меняется на код, который нужно отправить боту.
        required: 'Зарегистрируйтесь, написав §6{CODE}§r боту §6@mybot§r в Telegram.'
        # Сообщение об успешной регистрации.
        completed: '§a§lВы успешно зарегистрировались!'
        # Ответное сообщение при присылании неизвестного кода.
        tg-error: 'Неизвестный код. Попробуйте перезайти на сервер и получить новый.'
        # Сообщение об успешной регистрации (в диалоге), {PASS} меняется на пароль.
        tg-success:
          - 'Вы успешно зарегистрировались!'
          - 'Каждый вход будет требовать подтверждения в этом диалоге.'
          - ''
          - 'Для вас сгенерирован сложный пароль:'
          - '{PASS}'
          - ''
          - 'Вы можете использовать его в экстренных ситуациях.'
        # Сообщение при кике игрока, если он не зарегистрировался за указанное время.
        kick: '§c§lВремя регистрации истекло.'
      #  
      # Частота повторения `register.messages.required` в секундах.
      repeat: 4
      # Максимальное время для регистрации в секундах.
      timeout: 300
    #  
    login:
      messages:
        # Переодически повторяемое сообщение о необходимости авторизоваться.
        required: 'Авторизуйтесь, приняв запрос бота §6@mybot§r в Telegram.'
        # Сообщение об успешной авторизации.
        completed: '§a§lВы успешно авторизованы!'
        # Сообщение о неверном пароле.
        false: '§c§lНеправильный пароль.'
        # Сообщение при кике игрока, если он не авторизовался за указанное время.
        kick: '§c§lВремя авторизации истекло.'
        tg-request:
          # Сообщение владельцу аккаунта, {NICK} - ник, {IP} - IP адрес, откуда запрошен вход.
          message:
            - 'На ваш аккаунт ({NICK}) совершается попытка входа с IP-адреса {IP}.'
            - 'Если это вы, нажмите на :white_check_mark:, если нет - на :x:.'
            - 'Отклонение запроса мгновенно кикнет игрока с сервера.'
          # Текст кнопки "принять".
          button-1: ':white_check_mark:'
          # Текст кнопки "отклонить".
          button-2: ':x:'
          # Сообщение о принятии входа, {NICK} - ник, {IP} - IP адрес, откуда запрошен вход.
          accepted: ':white_check_mark: | Разрешён вход {NICK} с {IP}'
          # Сообщение об отклонении входа, {NICK} - ник, {IP} - IP адрес, откуда запрошен вход.
          rejected: ':x: | Отклонён вход {NICK} с {IP}'
          # Сообщение о том, что запрошенный игрок уже авторизован или вышел с сервера.
          offline: ':thinking:| Похоже, что игрок оффлайн или уже авторизован.'
          # Сообщение при кике игрока, если владелец аккаунта отклонил вход.
          kick: '§c§lВладелец аккаунта отклонил ваш вход.'
      #  
      # Частота повторения `login.messages.required` в секундах.
      repeat: 4
      # Максимальное время для авторизации в секундах.
      timeout: 40
      # Определяет, доступна ли команда /login.
      login-command-available: true
#  
#  
#  
# ┌──────────────────────────────────────────────────────────────────────────────────────────────────────────┐
# │                                      Управление твинками                                                 │
# │                                                                                                          │
# │ "Твинками" принято называть множественные аккаунты игрока.                                               │
# │ Эти настройки могут ограничить твинки игроков. Для этого сравниваются IP регистрации и последнего входа. │
# │ К уже зарегистрированным игрокам не применяются ограничения. Только новые аккаунты не смогут войти.      │
# │                                                                                                          │
# │ ВНИМАНИЕ: если у игрока есть 1 зарегистрированный аккаунт и он входит с другого,                         │
# │ "чистого" ника - это попытка создания 2-го твинка.                                                       │
# │                                                                                                          │
# │ То есть твинками считаются все аккаунты, в том числе и самый первый.                                     │
# └──────────────────────────────────────────────────────────────────────────────────────────────────────────┘
twinks:
  # Включена ли система ограничения твинков?
  enabled: true
  # Максимальное количество твинков, после которого новые аккаунты не будут допущены.
  max: 3
  # Сообщение при отклонении подключения.
  kick-message: '§c§lУ вас слишком много твинков!'
  #  
  # Настройки исключений.
  exceptions:
    # Список никнеймов, к которым не применяются проверки.
    nicks: [ ]
    # Список IP-адресов, к которым не применяются проверки.
    ips: [ ]
  #  
  # Настройки репортов о событиях, связанных с контролем твинков.
  report:
    # Репорт при пропуске твинка. То есть в момент, когда твинк допущен, потому что число ниже максимального.
    onJoin:
      # Включен ли репорт?
      enabled: true
      # Право для получения репорта. Консоль получит в любом случае.
      permission: 'baronessauth.admin'
      # Сообщение репорта. Заменители:
      #       {nick} - ник нового твинка,
      #       {ip} - IP-адрес, с которого происходит вход,
      #       {total} - новое количество твинков.
      message: 'Замечен новый твинк: §l§6{nick}§r. IP: §l§6{ip}§r. Всего твинков: §l§6{total}§r.'
      # От какого количества твинков работает репорт? Указывается количество с учётом нового.
      # То есть если у игрока уже есть 2 твинка и входит новый, количество - 3.
      min: 2
    #  
    # Репорт при отклонении твинка. То есть в момент, когда твинк кикнут.
    onKick:
      # Включен ли репорт?
      enabled: true
      # Право для получения репорта. Консоль получит в любом случае.
      permission: 'baronessauth.admin'
      # Сообщение репорта. Заменители:
      #       {nick} - ник нового твинка,
      #       {ip} - IP-адрес, с которого происходит вход,
      message: 'Предотвращён вход нового твинка: §l§6{nick}§r. IP: §l§6{ip}§r.'
#  
#  
#  
# ┌────────────────────────────────────────────────────────┐
# │                Настройки безопасности                  │
# └────────────────────────────────────────────────────────┘
security:
  #  
  # ┌────────────────────────────────────────────────────────┐
  # │            Настройки подключений игроков               │
  # └────────────────────────────────────────────────────────┘
  connections:
    #  
    # ┌───────────────────────────────────────────────────────────────────────────────┐
    # │                        Защита от "null ip"                                    │
    # │                                                                               │
    # │ Самая первая и самая "лёгкая" проверка, которая отсеивает подключения без IP. │
    # │                                                                               │
    # │ Обычные игроки никогда не попадутся на эту проверку, только злоумышленники.   │
    # │                                                                               │
    # │ Крайне не рекомендуем отключать эту защиту, так как "null ip"                 │
    # │ вызовет ошибки в работе BaronessAuth и других плагинов.                       │
    # └───────────────────────────────────────────────────────────────────────────────┘
    anti-null-ip:
      # Включена ли защита?
      enabled: true
      # Сообщение при отклонении подключения
      message: '§cВаш IP не распознан!'
    #  
    # ┌──────────────────────────────────────────────────────────────────────────────────┐
    # │                          Кулдаун на переподключение                              │
    # │                                                                                  │
    # │ Эта функция позволяет запретить игрокам слишком быстро перезаходить на сервер.   │
    # │                                                                                  │
    # │ Не тратит ресурсы сервера, поэтому рекомендуется использование                   │
    # │ во избежание спама через релог.                                                  │
    # └──────────────────────────────────────────────────────────────────────────────────┘
    join-cooldown:
      # Включен ли интервал входа?
      enabled: true
      # Время в секундах, через которое будет вновь доступен вход.
      time: 2
      # Сообщение при отклонении подключения.
      message: '§cВы слишком быстро перезаходите!'
    #  
    # ┌──────────────────────────────────────────────────────────────────────────────────┐
    # │                             Защита от Proxy/VPN                                  │
    # │                                                                                  │
    # │ Позволяет отклонять подключения, которые являются VPN, Proxy,                    │
    # │ Tor или дата-центром (последнее часто используется для ботов).                   │
    # │                                                                                  │
    # │ Для этого используется Lite база данных от сервиса IP2Location.                  │
    # │ Она автоматически обновляется и скачивается с наших серверов.                    │
    # └──────────────────────────────────────────────────────────────────────────────────┘
    anti-proxy:
      # Включена ли защита от Proxy/VPN?
      enabled: true
      # Включает режим "держать базу в ОЗУ".
      # Этот режим гораздо быстрее, так как не обращается к диску.
      # Траты небольшие, поэтому лучше включить, чтобы разгрузить накопитель.
      keep-in-ram: true
      # Сообщение при отклонении подключения.
      message: '§cВход с Proxy/VPN запрещён!'
      # IP-адреса, для которых проверка не осуществляется.
      whitelist:
        - '127.0.0.1'
    #  
    # ┌──────────────────────────────────────────────────────────────────────┐
    # │          Защита от "you logged in from another location"             │
    # │                                                                      │
    # │ Сервер, если входит игрок с ником, который уже занят другим игроком, │
    # │ кикает игрока с причиной "you logged in from another location".      │
    # │                                                                      │
    # │ Эта функция позволяет не впускать игрока, если его ник занят.        │
    # └──────────────────────────────────────────────────────────────────────┘
    prevent-another-location-kicking:
      # Включена ли защита?
      enabled: true
      # Сообщение при отклонении подключения.
      message: '§cИгрок с таким ником уже онлайн!'
    #  
    # ┌────────────────────────────────────────────────────────────────────────────────────┐
    # │                             Фильтр по странам                                      │
    # │                                                                                    │
    # │ Позволяет вам сделать чёрный\белый список стран для подключения к серверу.         │
    # │ Используется GeoIP база данных, которая скачивается и обновляется автоматически.   │
    # │                                                                                    │
    # │ ПОДСКАЗКА: Вы можете узнать страну происхождения IP, используя /auth stats ip [ip] │
    # └────────────────────────────────────────────────────────────────────────────────────┘
    forbidden-countries:
      # Включена ли защита?
      enabled: false
      # Сообщение при отклонении подключения.
      message: '§cВход с вашей страны запрещён!'
      # Превращает список в белый.
      # Все страны, кроме указанных, будут отклонены, иначе наоборот.
      whitelist-mode: true
      # Список стран.
      list:
        - 'Россия'
        - 'Украина'
        - 'Беларусь'
      # IP-адреса, для которых проверка не осуществляется.
      whitelist-ip:
        - '127.0.0.1'
  #  
  # ┌────────────────────────────────────────────────────────────┐
  # │             Настройки правил сложности пароля              │
  # └────────────────────────────────────────────────────────────┘
  password-complexity:
    #  
    # Минимальная длина пароля.
    # Пароли короче 4 символов крайне небезопасны. Чем больше, тем лучше.
    length:
      # Минимальная длина.
      minimum-length: 4
      # Сообщение об отклонении.
      message: '§c§lМинимальная длина пароля:§6§l 4 символа§c§l.'
    #  
    # Проверяет, использует ли игрок в качестве пароля собственный ник.
    # Да, многие игроки так делают... Это очень странно.
    same-nick:
      # Включена ли проверка?
      enabled: true
      # Сообщение об отклонении.
      message: '§cВы не можете в качестве пароля использовать собственный ник!'
    #  
    # Проверяет, был ли пароль слит, проверяя его по базе данных из более чем 600.000.000 вариантов.
    # Так как есть "ложные" сливы и вариантов слишком много, найти пароль с 0 сливами проблематично.
    # Для этого вы можете указать минимальное количество сливов.
    stolen:
      # Включена ли проверка?
      enabled: true
      # Минимальное количество сливов.
      minimum-count: 100000
      # Сообщение об отклонении, {count} - количество сливов.
      message: '§cЭтот пароль был слит §r{count}§c раз. Придумайте другой.'
    #  
    # Проверка пароля по регулярному выражению.
    # Не трогайте, если не разбираетесь.
    regex:
      # Включена ли проверка?
      enabled: false
      # Регулярное выражение
      custom-regex: "^[a-zA-Z0-9_]+$"
      # Сообщение об отклонении.
      message: '§c§lВаш пароль не прошёл проверку паттерна.'
  #  
  # ┌────────────────────────────────────────────────────────────────────────────────────────┐
  # │                              Защита от брутфорса                                       │
  # │                                                                                        │
  # │ Брутфорсом называют перебор паролей, благодаря которому можно подобрать пароль игрока. │
  # │ Эта функция способна блокировать попытки брутфорса.                                    │
  # │                                                                                        │
  # │ Если игрок слишком много раз введёт неправильный пароль,                               │
  # │ он будет кикнут и не сможет войти в течении определённого времени.                     │
  # └────────────────────────────────────────────────────────────────────────────────────────┘
  bruteforce:
    # Включена ли защита?
    enabled: true
    # Максимальное количество попыток.
    max-attempts: 3
    # Время блокировки в секундах.
    block-time: 60
    # Сообщение при кике и попытке подключиться во время блока.
    block-message: "Вы слишком часто вводите неправильный пароль! \n Попробуйте снова через минуту."
  #  
  # ┌────────────────────────────────────────────────────────────────────────────────────────┐
  # │                           Защита от подмены UUID                                       │
  # │                                                                                        │
  # │ Крайне важна для каждого сервера, так как, подменив UUID, можно взломать сервер.       │
  # │                                                                                        │
  # │ Например, подмена позволяет получить привилегии другого игрока (в том числе и админа). │
  # └────────────────────────────────────────────────────────────────────────────────────────┘
  uuid-protection:
    # Включена ли защита?
    enabled: true
    # Сообщение при отклонении подключения.
    message: '§c§lПодмена UUID запрещена.'
    # Настройки репорта об остановленной подмене.
    report:
      # Включен ли репорт?
      enabled: true
      # Право, владельцы которого получат репорт. Консоль получит в любом случае.
      permission: 'baronessauth.admin'
      # Сообщение с репортом, {nick} меняется на ник нарушителя.
      message: '§l§6{nick} пытался зайти с подменой UUID, но был остановлен.'
  #  
  # ┌────────────────────────────────────────────────────────────────────────────────────────┐
  # │                               Фильтр логов                                             │
  # │                                                                                        │
  # │ Фильтр логов, который не даёт чувствительной информации вроде                          │
  # │ "player issued server command: /login password" попасть в логи.                        │
  # │ Отфильтрованные записи не отображаются в консоли и не записываются в журнал сервера.   │
  # └────────────────────────────────────────────────────────────────────────────────────────┘
  log-filter:
    # Включён ли фильтр логов?
    enabled: true
#  
#  
#  
# ┌───────────────────────────────────────────────────────────────────────────────────────┐
# │                          Настройки отображения дат.                                   │
# │                                                                                       │
# │ Можно беспрепятственно менять, это никак не влияет на данные - только на отображение. │
# └───────────────────────────────────────────────────────────────────────────────────────┘
date-format:
  # Часовой пояс.
  timeZone: 'GMT+3'
  # Формат даты.
  # Подробнее: https://docs.baronessdev.ru/BaronessAuth/configuration/data/date
  format: 'yyyy.MM.dd HH:mm'
#  
#  
#  
# ┌──────────────────────────────────────────────┐
# │ Фильтр ников на этапе подключения к серверу. │
# └──────────────────────────────────────────────┘
nick-filter:
  # Включён ли фильтр?
  # Главный параметр, при отключении игнорируются все остальные.
  enabled: true
  # Сообщение при отклонении подключения.
  kickMessage: '§cВы не прошли проверку ника.'
  #  
  # ┌──────────────────────────────────────────────────────────┐
  # │ Запрещает игроку менять регистр ника после регистрации.  │
  # │ То есть Black_Baroness не сможет войти с BlaCK_bARONEsS. │
  # └──────────────────────────────────────────────────────────┘
  checkCase: true
  #  
  # ┌────────────────────────────────────────────┐
  # │ "Классический" фильтр.                     │
  # │ Запрещает всё, кроме латиницы, цифр и "_". │
  # └────────────────────────────────────────────┘
  classic: true
  #  
  # ┌──────────────────────┐
  # │ Блокирует кириллицу. │
  # └──────────────────────┘
  blockCyrillic: false
  #  
  # ┌────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  # │ Список из запрещенных символов и сочетаний. Например:                                                          │
  # │ - '#'                                                                                                          │
  # │ - '^_^'                                                                                                        │
  # │ Количество символов в каждом правиле не ограничено.                                                            │
  # │ К примеру, вы можете запретить "plant" и игроки plantguy, iloveplants2006 и average_plant_fun не смогут войти. │
  # └────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
  customFilter: [ ]
  #  
  # ┌────────────────────────────────────┐
  # │ Фильтр по регулярному выражению.   │
  # │ Не трогайте, если не разбираетесь. │
  # └────────────────────────────────────┘
  regex:
    # Включён ли фильтр.
    enabled: false
    # Регулярное выражение.
    rule: "^[a-zA-Z0-9_]+$"
  #  
  # ┌───────────────────────┐
  # │ Фильтр по длине ника. │
  # └───────────────────────┘
  length:
    # Минимальная длина.
    min: 4
    # Максимальная длина.
    max: 16
#  
#  
#  
# ┌──────────────────────────────────────────────────────────────────────────────────────────────────────┐
# │ Плагин при включении проверяет, совпадает ли хеш-сумма его файла с той, что опубликовал разработчик. │
# │ Это позволяет вам удостовериться, что установленный плагин оригинальный и не модифицирован.          │
# └──────────────────────────────────────────────────────────────────────────────────────────────────────┘
validate:
  # Выключать ли сервер, если хеш-сумма не совпадает?
  # Это не влияет на видимость предупреждения.
  # Имейте в виду, если плагин заражен, ваш сервер в потенциальной опасности!
  shutdown-server: true
#  
#  
#  
# ┌───────────────────────────────────────────────────┐
# │               Настройки заморозки                 │
# └───────────────────────────────────────────────────┘
freeze:
  #  
  # Настройки блокировки движения.
  move:
    # Блокировать ли движения?
    preventMove: true
    # Режим заморозки.
    #   |=> legacy   используемый у конкурентов, довольно тяжелый алгоритм.
    #   |=> spigot   уникальный алгоритм, использующий сброс скорости и фальшивые блоки.
    #   |=> paper    уникальный алгоритм, работающий только с Paper API (Paper и его форки). Самый быстрый.
    #   |=> auto     paper, если доступен, иначе spigot.
    # Для изменения полностью перезагрузите сервер.
    mode: auto
    #  
    # Настройки блокировки падения.
    preventFall:
      # Блокировать ли падение?
      enabled: true
      # Периодичность проверки в тиках. Высокие значения снизят нагрузку на сервер, но могут вызвать ошибки блокировки.
      period: 1
    #  
    # Настройки фальшивых блоков.
    fake-block-settings:
      # ID блока, который используется для создания фальшивых блоков.
      block: BARRIER
      # Периодичность обновления в тиках. Высокие значения снизят нагрузку на сервер, но могут вызвать ошибки блокировки.
      period: 1
  #  
  # Настройки блокировки взаимодействия игрока с окружением.
  interact:
    # Нужно ли блокировать взаимодействия? Отключение отключит все прочие настройки.
    blocking: true
    # Нужно ли блокировать клик ЛКМ по блокам?
    block-left-click-block: true
    # Нужно ли блокировать клик ПКМ по блокам?
    block-right-click-block: true
    # Нужно ли блокировать клик ЛКМ по воздуху?
    block-left-click-air: true
    # Нужно ли блокировать клик ПКМ по воздуху?
    block-right-click-air: true
    # Нужно ли блокировать физические взаимодействия?
    # Подробнее: https://hub.spigotmc.org/javadocs/spigot/org/bukkit/event/block/Action.html#PHYSICAL
    block-physical-interact: true
    # Нужно ли блокировать взаимодействия с сущностями?
    # В это входят ПКМ, ЛКМ, толкания и прочее.
    block-entity-interact: true
  #  
  # Настройки взаимодействия с чатом.
  chat:
    # Блокировать отправку сообщений в чат?
    block-send: true
    # Блокировать ли получение сообщений в чате?
    # Может не блокировать, если ваш плагин на чат нехорошо написан.
    # [!!!] Сейчас временно не действует.
    block-receive: false

  # Блокировать ли получение урона?
  block-damage: true
  # Блокировать ли Tab-Complete?
  # Не работает на очень старых версиях.
  block-tab-complete: true
  # Блокирует все команды, кроме /l, /login, /reg, /register.
  block-commands: true
  # Блокировать ли выбрасывание предметов?
  block-drop: true
  # Блокировать ли поднятие предметов?
  block-pickup: true
  # Блокировали ли отправку projectile? Это стрелы, снежки и тому подобное.
  block-projectile: true
  # Блокировать ли телепортацию? Срабатывает только если чанк назначения отличается от текущего.
  block-teleport: true
  # Блокировать ли восстановление здоровья?
  block-heal: true
  # Блокировать ли клики в инвентаре? Относится к любым инвентарям.
  block-inventory-click: true
#  
#  
#  
# ┌────────────────────────────────────────────────┐
# │ Настройки команд, предназначенных для игроков. │
# └────────────────────────────────────────────────┘
commands:
  #  
  # ┌──────────────────────────────────────────────────────────────┐
  # │ Команда /register.                                           │
  # │ Она используется для регистрации через некоторые провайдеры. │
  # └──────────────────────────────────────────────────────────────┘
  register:
    # Tab-Complete для команды. Для изменения требуется полная перезагрузка сервера.
    # Чтобы отключить, установите [].
    tab-complete: [ 'пароль' ]
    # Алиасы (варианты) команды. Для изменения требуется полная перезагрузка сервера.
    aliases: [ 'register', 'reg' ]
  #  
  # ┌──────────────────────────────────────────────────────────────┐
  # │ Команда /login.                                              │
  # │ Она используется для авторизации через некоторые провайдеры. │
  # └──────────────────────────────────────────────────────────────┘
  login:
    # Tab-Complete для команды. Для изменения требуется полная перезагрузка сервера.
    tab-complete: [ 'пароль' ]
    # Алиасы (варианты) команды. Для изменения требуется полная перезагрузка сервера.
    aliases: [ 'login', 'l' ]
  #  
  # ┌───────────────────────────────────────────────────────────────────────────────┐
  # │ Команда /changepassword.                                                      │
  # │ Она используется для смены пароля. Синтаксис: [старый пароль] [новый пароль]. │
  # │ Проводятся все проверки на сложность пароля, как и при регистрации.           │
  # └───────────────────────────────────────────────────────────────────────────────┘
  changepassword:
    # Включена ли команда? Если нет, она будет недоступна.
    enabled: true
    # Право для использования команды. Чтобы отключить, установите ''.
    permission: 'baronessauth.changepassword'
    # Tab-Complete для команды. Для изменения требуется полная перезагрузка сервера.
    # Чтобы отключить, установите [].
    tab-complete: [ ]
    # Алиасы (варианты) команды. Для изменения требуется полная перезагрузка сервера.
    aliases: [ 'changepassword' ]
    #  
    # Сообщения.
    messages:
      # Подсказка об использовании команды.
      usage: 'Использование: §6/changepassword [старый пароль] [новый пароль]§r.'
      # Сообщение при попытке использовать команду, если та выключена.
      disabled: '§c§lВозможность смены пароля отключена.'
      # Сообщение об успешной смене пароля.
      success: '§a§lВы успешно сменили пароль.'
      # Сообщение о неверном пароле
      false: '§c§lНеверный пароль.'
  #  
  # ┌───────────────────────────────────────────┐
  # │ Команда /logout.                          │
  # │ Она используется для сброса своей сессии. │
  # │ После сброса игрок будет кикнут.          │
  # └───────────────────────────────────────────┘
  logout:
    # Включена ли команда? Если нет, она будет недоступна.
    enabled: true
    # Право для использования команды. Чтобы отключить, установите ''.
    permission: 'baronessauth.logout'
    # Алиасы (варианты) команды. Для изменения требуется полная перезагрузка сервера.
    aliases: [ 'logout' ]
    #  
    # Сообщения.
    messages:
      # Сообщение при попытке использовать команду, если та выключена.
      disabled: '§c§lВозможность смены пароля отключена.'
      # Сообщение при кике.
      kick-message: 'Ваша сессия сброшена.'
#  
#  
#  
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                          Настройки хеширования паролей.                     │
# │ Подробнее: https://docs.baronessdev.ru/BaronessAuth/configuration/data/hash │
# └─────────────────────────────────────────────────────────────────────────────┘
hashing:
  # Список из алгоритмов, которые выполняется поочередно.
  algorithm:
    - 'name Tiger128'
    - 'Tiger128'
    - 'SALT'
    - 'Tiger128'
  # Длина генерируемой динамической соли. Больше - лучше.
  salt-length: 20
#  
#  
#  
# ┌──────────────────────────────────────────────────────────┐
# │ Настройки автоматических резервных копий базы данных.    │
# │ Для этого создаются дампы (подробнее в субкоманде dump). │
# └──────────────────────────────────────────────────────────┘
backup:
  # Включены ли автоматические бэкапы?
  enabled: true
  # Частота повторения в секундах.
  timer: 3600
#  
#  
#  
# ┌──────────────────────────────────────────────────────────────────────────────────────────┐
# │                                  Настройки сессий.                                       │
# │ Это функция, которая позволяет игроку не вводить пароль повторно при входе с того же IP. │
# └──────────────────────────────────────────────────────────────────────────────────────────┘
session:
  # Включены ли сессии?
  enabled: true
  # Длительность действия сессии в секундах. Если время кончилось, придётся входить снова.
  duration: 3600
  # Сообщение, которое видит игрок, если ему не требуется вход из-за наличия сессии.
  message: '§a§lСессия сохранена, ввод пароля не требуется.'
#  
#  
#  
# ┌───────────────────────────────────────────────────────────────────────────────┐
# │                                   Логи в файл                                 │
# │                                                                               │
# │ Логирование разных действий игроков.                                          │
# │ Все логи сохраняются в файл .log своей категории, в папке /BaronessAuth/logs. │
# │ Каждая запись содержит минимум ник и IP игрока.                               │
# └───────────────────────────────────────────────────────────────────────────────┘
logs:
  # Успешная регистрация. Указывает хеш пароля.
  registration: false
  # Неудачная авторизация. Указывает хеш правильного пароля и введённый неправильный.
  login-failure: false
  # Успешная авторизация. Указывает хеш правильного пароля и введённый неправильный.
  login-success: false
  # Мгновенная авторизация по активной сессии.
  session-saved: false
  # Смена пароля через changepassword. Указывает хеши старого и нового паролей.
  changepassword: false
  # Временный бан в качестве защиты от брутфорса. Указывает текущее максимальное количество попыток.
  bruteforce: false
#  
#  
#  
# ┌──────────────────────────────────────────────────────┐
# │ Настройки команд, предназначенных для администрации. │
# └──────────────────────────────────────────────────────┘
admin-commands:
  #  
  #  
  #  
  # ┌────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  # │ Основная команда администрации.                                                                            │
  # │                                                                                                            │
  # │ Сама из себя ничего не представляет, будучи связующим компонентом множества субкоманд.                     │
  # │ [!!!] Обязательно к прочтению: https://docs.baronessdev.ru/BaronessAuth/configuration/adminCommands/basics │
  # └────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
  auth:
    # Право для команды. Для изменения требуется полная перезагрузка сервера.
    permission: 'baronessauth.admin'
    # Алиасы (варианты) команды. Для изменения требуется полная перезагрузка сервера.
    aliases: [ 'auth' ]
    #  
    # Предположение о субкоманде.
    # При попытке вызвать несуществующую субкоманду, BaronessAuth попытается предложить вам похожую.
    guessing:
      # Включено ли предположение?
      enabled: true
      # Сообщение о предположении, {maybe} меняется на похожую субкоманду.
      message: 'Субкоманда не найдена. Может быть, вы имели ввиду §l§6{maybe}§f?'
    #  
    # ┌──────────────────────┐
    # │ Настройки субкоманд. │
    # └──────────────────────┘
    subcommands:
      #  
      # ┌──────────────────────────────────────────────────────────────────────────────────────────┐
      # │ Субкоманда "gui".                                                                        │
      # │                                                                                          │
      # │ Открывает меню администратора.                                                           │
      # │ В меню находятся иконки, создаваемые самим BaronessAuth или другими плагинами через API. │
      # └──────────────────────────────────────────────────────────────────────────────────────────┘
      gui:
        # Включена ли субкоманда?
        enabled: true
        # Право для субкоманды.
        permission: 'baronessauth.admin'
        # Алиасы (варианты) субкоманды.
        aliases: [ 'gui' ]
        # Описание субкоманды.
        description: '- открывает GUI'
        # Заголовок меню.
        title: 'Панель администратора BaronessAuth'
        # Размер меню. Требования:
        # - Больше 9. Если меньше, установится 9.
        # - Меньше 54. Если больше, установится 54.
        # - Кратен 9. Если нет, будет установлено ближайшее меньшее (если 52, то 49)
        size: 54
      #  
      # ┌────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
      # │ Субкоманда "reload".                                                                                       │
      # │                                                                                                            │
      # │ Перезагружает плагин.                                                                                      │
      # │ [!!!] Обязательно к прочтению: https://docs.baronessdev.ru/BaronessAuth/configuration/adminCommands/reload │
      # └────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
      reload:
        # Включена ли субкоманда?
        enabled: true
        # Право для субкоманды.
        permission: 'baronessauth.admin'
        # Алиасы (варианты) субкоманды.
        aliases: [ 'reload' ]
        # Описание субкоманды.
        description: '- перезагружает плагин'
        # Сообщение об успешной перезагрузке плагина.
        reloaded: '§aПлагин перезагружен'
        #  
        icon:
          # Заголовок иконки в GUI.
          title: '§aПерезагрузка'
          # Описание иконки в GUI.
          lore:
            - '§fПолностью перезагружает плагин'
          # Материал иконки в GUI. Не трогайте, если не разбираетесь.
          material: 'CACTUS'
      #  
      # ┌───────────────────────────────────────────────────────────────────────────────────────┐
      # │ Субкоманда "stats".                                                                   │
      # │                                                                                       │
      # │ Позволяет получать различную статистику.                                              │
      # │ Подробнее: https://docs.baronessdev.ru/BaronessAuth/configuration/adminCommands/stats │
      # └───────────────────────────────────────────────────────────────────────────────────────┘
      stats:
        # Включена ли субкоманда?
        enabled: true
        # Право для субкоманды.
        permission: 'baronessauth.admin'
        # Алиасы (варианты) субкоманды.
        aliases: [ 'stats' ]
        # Описание субкоманды.
        description: '[total/nick/ip] - статистика'
        #  
        # ┌────────────────┐
        # │ Режим "total". │
        # └────────────────┘
        total:
          # Так как запрос по большой базе данных может привести к зависанию сервера,
          # сбор статистики ведётся в отдельном потоке и может занять время.
          # Это сообщение информирует об этом.
          another-thread-started: 'Статистика в процессе сбора. Подождите, пожалуйста.'
          # Если поток сбора активен, а игрок повторяет команду.
          another-thread-active: 'Сбор статистики уже начат и ведётся асинхронно. Подождите, пожалуйста.'
          # Сообщения в режиме "total".
          # {online} - игроки онлайн.
          # {login_today} - количество уникальных авторизаций за сегодня.
          # {login_mount} - количество уникальных авторизаций за последние 30 дней.
          # {reg_today} - количество регистраций за сегодня.
          # {reg_mount} - количество регистраций за последние 30 дней.
          # {reg_total} - количество регистраций за всё время.
          messages:
            - 'Сейчас онлайн: §6{online}'
            - 'Входов за сегодня: §6{login_today}'
            - 'Входов за месяц: §6{login_mount}'
            - 'Зарегистрировано за сегодня: §6{reg_today}'
            - 'Зарегистрировано за месяц: §6{reg_mount}'
            - 'Зарегистрировано за всё время: §6{reg_total}'
        #  
        # ┌───────────────┐
        # │ Режим "nick". │
        # └───────────────┘
        nick:
          # Подсказка об использовании.
          usage: 'Использование: §6/auth stats nick [ник]'
          # Сообщение о том, что игрок с таким ником не зарегистрирован.
          error: '§cИгрок не найден.'
          # Статус "онлайн".
          online: '§aОнлайн'
          # Статус "оффлайн".
          offline: '§cНе в сети'
          # Сообщения режима.
          # {nick} - "родной" ник игрока.
          # {reg_date} - дата регистрации.
          # {last_date} - дата последней авторизации.
          # {reg_ip} - IP регистрации.
          # {reg_ip_country} - Страна регистрации.
          # {reg_ip_city} - Город регистрации.
          # {last_ip} - IP последней авторизации.
          # {last_ip_country} - Страна последней авторизации.
          # {last_ip_city} - Город последней авторизации.
          # {uuid} - "родной" UUID игрока.
          # {status} - статус (онлайн или оффлайн).
          messages:
            - '§6>>> Информация об игроке §f{nick}§6 <<<'
            - 'Дата регистрации: §6{reg_date}'
            - 'Дата последнего входа: §6{last_date}'
            - 'Зарегистрирован с IP: §6{reg_ip}§f (§6{reg_ip_country}§f, §6{reg_ip_city}§f)'
            - 'Последний IP: §6{last_ip}§f (§6{last_ip_country}§f, §6{last_ip_city}§f)'
            - 'UUID: §6{uuid}'
            - 'Статус: §6{status}'
        #  
        # ┌─────────────┐
        # │ Режим "ip". │
        # └─────────────┘
        ip:
          # Подсказка об использовании.
          usage: 'Использование: §6/auth stats ip [ip]'
          # Информация о местоположении IP.
          # {country} - страна.
          # {city} - город.
          geolocation: 'Местоположение: §6{country}§f, §6{city}§f'
          #  
          register:
            # Сообщение при отсутствии регистраций.
            empty: '§cРегистраций с этого IP не найдено.'
            # Заголовок списка регистраций, {count} - количество.
            header: 'Найдено §6{count}§f регистраций с этого IP:'
          #  
          login:
            # Сообщение при отсутствии авторизации.
            empty: '§cПоследних авторизаций с этого IP не найдено.'
            # Заголовок списка авторизаций, {count} - количество.
            header: 'Найдено §6{count}§f последних авторизаций с этого IP:'
          #  
          # Выводить ли списки?
          listEnabled: true
          # Формат списков, {nick} - ник игрока.
          listFormat: '> {nick}'
      #  
      # ┌────────────────────────────────────────────────────────────────┐
      # │ Субкоманда "setpassword".                                      │
      # │                                                                │
      # │ Позволяет установить пароль любому зарегистрированному игроку. │
      # │ Никаких проверок. Вы можете установить абсолютно любой пароль. │
      # │ Синтаксис: [игрок] [новый пароль].                             │
      # └────────────────────────────────────────────────────────────────┘
      setpassword:
        # Включена ли субкоманда?
        enabled: true
        # Право для субкоманды.
        permission: 'baronessauth.admin'
        # Алиасы (варианты) субкоманды.
        aliases: [ 'setpassword' ]
        # Описание субкоманды.
        description: '[игрок] [пароль] - устанавливает пароль игроку'
        # Сообщение о том, что игрок не найден.
        not-found: '§cИгрок не найден.'
        # Сообщение об успехе.
        success: '§aВы успешно сменили пароль игроку.'
      #  
      # ┌──────────────────────────────────────────────────────────────┐
      # │ Субкоманда "unregister".                                     │
      # │                                                             │
      # │ Позволяет безвозвратно удалить данные игрока из базы данных. │
      # │ Синтаксис: [игрок]                                           │
      # └──────────────────────────────────────────────────────────────┘
      unregister:
        # Включена ли субкоманда?
        enabled: true
        # Право для субкоманды.
        permission: 'baronessauth.admin'
        # Алиасы (варианты) субкоманды.
        aliases: [ 'unregister' ]
        # Описание субкоманды.
        description: '[игрок] - безвозвратно удаляет данные игрока из базы данных'
        # Сообщение о том, что игрок не найден.
        not-found: '§cИгрок не найден.'
        # Сообщение о том, что игрок не найден.
        online: '§cИгрок онлайн, операция невозможна.'
        # Сообщение об успехе.
        success: '§aВы успешно удалили данные игрока.'
      #  
      # ┌────────────────────────────────────────────────────────────────────────────────┐
      # │ Субкоманда "unfreeze".                                                         │
      # │                                                                                │
      # │ Позволяет снять любой "запрос" с игрока. Обычно это приводит к его разморозке. │
      # │ Синтаксис: [игрок]                                                             │
      # └────────────────────────────────────────────────────────────────────────────────┘
      unfreeze:
        # Включена ли субкоманда?
        enabled: true
        # Право для субкоманды.
        permission: 'baronessauth.admin'
        # Алиасы (варианты) субкоманды.
        aliases: [ 'unfreeze' ]
        # Описание субкоманды.
        description: '[игрок] - снимает с игрока заморозку'
        # Сообщение о том, что игрок не найден или не заморожен.
        error: '§cИгрок не найден \ не заморожен.'
        # Сообщение об успехе.
        success: '§aВы разморозили игрока.'
      #  
      # ┌───────────────────────────────────────────────────────────────────────────────┐
      # │ Субкоманда "migrate".                                                         │
      # │                                                                               │
      # │ Позволяет совершить переезд с других плагинов.                                │
      # │ Обычно это происходит автоматически, достаточно только выбрать нужный плагин. │
      # │ Подробности конкретного каждого плагина есть на https://docs.baronessdev.ru.  │
      # └───────────────────────────────────────────────────────────────────────────────┘
      migrate:
        # Включена ли субкоманда?
        enabled: true
        # Право для субкоманды.
        permission: 'baronessauth.admin'
        # Алиасы (варианты) субкоманды.
        aliases: [ 'migrate' ]
        # Описание субкоманды.
        description: '[плагин] - переносит данные с другого плагина'
        # Сообщение о том, что сейчас уже активен другой перенос.
        locked: 'Вы не можете начать перенос, если активен предыдущий.'
        # Сообщение при вызове команды без аргументов \ с невалидными аргументами.
        # {list} меняется на список доступных в данный момент переносов.
        available: 'Доступные переносы: {list}'
        # Сообщение о начале процесса переноса.
        processing: 'Перенос начат. Подробности о процессе отображаются в консоли.'
        # Скрывать ли логи консоли в стиле "%player% migrated"?
        hide: false
      #  
      # ┌───────────────────────────────────────────────────────────────────────────────────┐
      # │ Субкоманда "load".                                                                │
      # │                                                                                   │
      # │ Выводит различную техническую информацию вашей системы.                           │
      # │ Обращается напрямую к ОС, а не к JVM, благодаря этому, информация крайне точна.   │
      # │ ВНИМАНИЕ: работает только на Linux. На других системах автоматически отключается. │
      # └───────────────────────────────────────────────────────────────────────────────────┘
      load:
        # Включена ли субкоманда?
        enabled: true
        # Право для субкоманды.
        permission: 'baronessauth.admin'
        # Алиасы (варианты) субкоманды.
        aliases: [ 'load' ]
        # Описание субкоманды.
        description: '- отображает техническую статистику'
        # Сообщения с информацией.
        #   |=> {os_distro}    - дистрибутив
        #   |=> {os_version}   - версия ядра Linux
        #   |=> {java_version} - версия Java
        #   |=> {cpu_name}     - модель процессора
        #   |=> {cpu_load}     - суммарная нагрузка процессора (учёт всех ядер)
        #   |=> {ram_total}    - общее количество ОЗУ
        #   |=> {ram_used}     - количество занятой ОЗУ (SWAP не считается занятостью)
        #   |=> {ram_used_%}   - количество занятой ОЗУ в процентах (SWAP не считается занятостью)
        #   |=> {ram_free}     - количество свободной ОЗУ (SWAP считается свободной памятью)
        #   |=> {ram_free_%}   - количество свободной ОЗУ в процентах (SWAP считается свободной памятью)
        #   |=> {db_ping}      - время в мс, нужное для исполнения запроса в базу данных
        #   |=> {db_name}      - название базы данных
        #   |=> {task_active}  - количество активных асинхронных задач
        #   |=> {task_queue}   - количество асинхронных задач в очереди
        messages:
          - '§7Операционная система:'
          - '§7== Дистрибутив: §l§6{os_distro}'
          - '§7== Версия ядра: §l§6{os_version}'
          - '§7== Java: §l§6{java_version}'
          - '§7Процессор:'
          - '§7== Модель: §l§6{cpu_name}'
          - '§7== Нагрузка: §l§6{cpu_load}§7%'
          - '§7Оперативная память:'
          - '§7== Всего: §l§6{ram_total}'
          - '§7== Занято: §l§6{ram_used}§7 ({ram_used_%}§7%)'
          - '§7== Свободно: §l§6{ram_free}§7 ({ram_free_%}§7%)'
          - '§7Производительность:'
          - '§7== Пинг БД: {db_ping}§7ms (§l§6{db_name}§7)'
          - '§7== Активных задач: §l§6{task_active}'
          - '§7== Задач в очереди: §l§6{task_queue}'
      #  
      # ┌────────────────────────────────────────────────────────────────────────────────────────┐
      # │ Субкоманда "dump"                                                                      │
      # │                                                                                        │
      # │ Позволяет создавать гибкие резервные копии базы данных.                                │
      # │                                                                                        │
      # │ Это очень полезно - вы можете создать дамп на одной базе данных                        │
      # │ и без проблем импортировать его в другую.                                              │
      # │                                                                                        │
      # │ Кроме того, это можно использовать в качестве удобных резервных копий,                 │
      # │ которые можно восстановить в случае надобности.                                        │
      # │                                                                                        │
      # │ |=> save - создаёт новый файл в /BaronessAuth/dump/, чьё имя базируется                │
      # │ на текущей дате (по вашему формату). Всё, кроме чисел меняется на "-".                 │
      # │ После создания, файл заполняется текущими данными. Это может занять некоторое время.   │
      # │                                                                                        │
      # │ |=> load [файл] - загружает данные из файла, игнорирует конфликтующие записи           │
      # │                                                                                        │
      # │ |=> forceload [файл] - загружает данные из файла, пезаписывает конфликтующие записи    │
      # │                                                                                        │
      # │ Конфликтующие записи это записи, которые уже находятся в базе данных. К примеру,       │
      # │ попытка импортировать игрока Black_Baroness, когда таковой уже есть в базе - конфликт. │
      # └────────────────────────────────────────────────────────────────────────────────────────┘
      dump:
        # Включена ли субкоманда?
        enabled: true
        # Право для субкоманды.
        permission: 'baronessauth.admin'
        # Алиасы (варианты) субкоманды.
        aliases: [ 'dump' ]
        # Описание субкоманды.
        description: '- создание и загрузка дампов базы данных'
        #  
        # Подсказка.
        help:
          - '/auth dump save - создать новый дамп'
          - '/auth dump load [файл] - загрузить дамп, игнорируя конфликты'
          - '/auth dump forceload [файл] - загрузить дамп, перезаписывая конфликты'
        #  
        # Начало сохранения.
        save-start: 'Дамп создаётся в отдельном потоке. Больше информации в консоли.'
        #  
        # Начало загрузки.
        load-start: 'Дамп загружается в отдельном потоке. Больше информации в консоли.'
        #  
        # Начало загрузки.
        file-not-exists: 'Указанный файл не существует'
      #  
      # ┌───────────────────────────────────────────────────────────────────────────────────────────┐
      # │ Субкоманда "developer".                                                                   │
      # │                                                                                           │
      # │ Особая команда, которая выполняет различные особые функции.                               │
      # │ Только базовые настройки. Сообщения изменить нельзя.                                      │
      # │ Подробнее: https://docs.baronessdev.ru/BaronessAuth/configuration/adminCommands/developer │
      # └───────────────────────────────────────────────────────────────────────────────────────────┘
      developer:
        # Включена ли субкоманда?
        enabled: true
        # Право для субкоманды.
        permission: 'baronessauth.developer'
        # Алиасы (варианты) субкоманды.
        aliases: [ 'developer' ]
        # Описание субкоманды.
        description: '- инструменты разработчика'
#  
#  
#  
# ┌───────────────────────────────────────────────────────────────────────────────────────────────┐
# │ Скрипты, позволяющие выполнять список команд при определённых событиях.                       │
# │ {PLAYER} меняется на ник игрока.                                                              │
# │                                                                                               │
# │ Пример:                                                                                       │
# │ on-register-required:                                                                         │
# │ - say {PLAYER} должен зарегистрироваться                                                      │
# │ - say Помогите ему, если что                                                                  │
# │ Этот пример пишет несколько сообщений в чат через say, когда игроку нужно зарегистрироваться. │
# └───────────────────────────────────────────────────────────────────────────────────────────────┘
scripts:
  # Игроку нужна регистрация.
  on-register-required: [ ]
  # Игрок завершил регистрацию.
  on-register-complete: [ ]
  # Игроку нужна авторизация.
  on-login-required: [ ]
  # Игрок завершил авторизацию. ПРИ РЕГИСТРАЦИИ ТОЖЕ ВЫЗЫВАЕТСЯ.
  on-login-complete: [ ]
  # Игрок вошёл с сохранённой сессией.
  on-session-saved: [ ]
  # Игрок вышел, не зарегистрировавшись.
  on-quit-without-register: [ ]
  # Игрок вышел, не авторизовавшись.
  on-quit-without-login: [ ]
#  
#  
#  
# ┌──────────────────────────────────────────────────┐
# │ Настройки дополнительного логирования в консоль. │
# └──────────────────────────────────────────────────┘
debug:
  # Выводить ли дополнительную информацию?
  enabled: true
  # Логировать ли DDL запросы в базу данных?
  sql-ddl: false
  # Логировать ли DML запросы в базу данных?
  sql-dml: false
#  
#  
#  
# ┌───────────────────────────────────────────────────────────────────────────────────────────────────┐
# │ Удобная подсказка об обновлениях через BaronessCloud.                                             │
# │ Позволяет настроить BaronessCloud так, что он будет сообщать об обновлениях не при входе, а после │
# │ вызова ивентов из списка.                                                                         │
# └───────────────────────────────────────────────────────────────────────────────────────────────────┘
update-notify:
  # Включена ли подсказка?
  enabled: true
  # Список ивентов, при вызове которых будет приходить уведомление об обновлениях.
  # Каждый класс обязан иметь публичный метод getPlayer(),
  # который возвращает org.bukkit.entity.Player.
  events:
    - 'ru.baronessdev.paid.auth.api.events.AuthPlayerLoginEvent'
    - 'ru.baronessdev.paid.auth.api.events.AuthPlayerSessionSavedEvent'
```
</details>

<details>  
<summary>database.yml</summary>

```yaml
# ┌───────────────────────────────────────────────┐
# │                                               │
# │            Настройки базы данных              │
# │                                               │
# └───────────────────────────────────────────────┘
#
# Выбранная база данных.
# Доступные варианты и их описание доступны в документации: http://docs.baronessdev.ru/BaronessAuth/configuration/database
database: HSQLDB
connection:
  # Адрес (IP).
  address: 'localhost'
  # Порт. Если указывать не нужно, выставьте 0 или меньше.
  port: 3306
  # Имя базы данных.
  db-name: 'db'
  # Пользователь.
  user: 'root'
  # Пароль.
  password: 'password'
  # Аргументы подключения.
  # Не трогайте, если не разбираетесь.
  args:
    - 'serverTimezone=Europe/Moscow'
    - 'autoReconnect=true'
    - 'useSSL=false'
settings:
  # Название таблицы с данными.
  table: 'ba_users'
  # Должен ли BaronessAuth выключать сервер, если подключение не удалось?
  shutdown-on-fail: true
  # Сколько секунд после выхода игрока нужно держать профиль игрока в кэше после его выхода?
  # Уменьшение ускорит очистку ОЗУ, но может создать дополнительную нагрузку на базу данных при повторном входе.
  cache-time: 30
```
</details>

<details>  
<summary>antibot.yml</summary>

```yaml
# ┌─────────────────────────────────────────────────────────────────────────────────┐
# │                                    Антибот                                      │
# │                                                                                 │
# │ Позволяет блокировать ботов на этапе регистрации.                               │
# │                                                                                 │
# │ Подробнее: http://docs.baronessdev.ru/BaronessAuth/configuration/antibot/basics │
# └─────────────────────────────────────────────────────────────────────────────────┘
#  
# Список включённых антибот-проверок.
# Можно указывать одну и ту же проверку несколько раз.
# Плагин выполняет проверки по очереди, следуя вашему сценарию.
enabled-challenges:
  - 'captcha'
  - 'click'
#  
# Проверка "капча в чате".
# Подробнее: http://docs.baronessdev.ru/BaronessAuth/configuration/antibot/captcha
captcha:
  # Сообщение о необходимости ввести капчу.
  message: 'Введите ответ на пример в чат §6числом§r.'
  # Варианты символов и чисел.
  symbols:
    # Варианты символа "+".
    +:
      - - '     '
        - '     '
        - ' +   '
        - '     '
        - '     '
      - - '     '
        - '  +  '
        - '     '
        - '     '
        - '     '
    # Варианты символа "-".
    "-":
      - - '     '
        - '     '
        - '  -  '
        - '     '
        - '     '
      - - '     '
        - '     '
        - '     '
        - ' -   '
        - '     '
    # Варианты числа 1.
    1:
      - - '    '
        - '    '
        - '    '
        - ' 1  '
        - '    '
      - - '    '
        - '   1'
        - '    '
        - '    '
        - '    '
    # Варианты числа 2.
    2:
      - - '    '
        - '   2'
        - '    '
        - '    '
        - '    '
      - - '    '
        - '    '
        - '    '
        - '    '
        - '  2 '
    # Варианты числа 3.
    3:
      - - '    '
        - '    '
        - ' 3  '
        - '    '
        - '    '
      - - '    '
        - '   3'
        - '    '
        - '    '
        - '    '
    # Варианты числа 4.
    4:
      - - '    '
        - '    '
        - ' 4  '
        - '    '
        - '    '
      - - '    '
        - '4   '
        - '    '
        - '    '
        - '    '
    # Варианты числа 5.
    5:
      - - '    '
        - ' 5  '
        - '    '
        - '    '
        - '    '
      - - '    '
        - '    '
        - '    '
        - '   5'
        - '    '
    # Варианты числа 6.
    6:
      - - '    '
        - '    '
        - ' 6  '
        - '    '
        - '    '
      - - '   6'
        - '    '
        - '    '
        - '    '
        - '    '
    # Варианты числа 7.
    7:
      - - '    '
        - '    '
        - '    '
        - ' 7  '
        - '    '
      - - '    '
        - '   7'
        - '    '
        - '    '
        - '    '
    # Варианты числа 8.
    8:
      - - '    '
        - '    '
        - ' 8  '
        - '    '
        - '    '
      - - '8   '
        - '    '
        - '    '
        - '    '
        - '    '
    # Варианты числа 9.
    9:
      - - '    '
        - '    '
        - ' 9  '
        - '    '
        - '    '
      - - '    '
        - '    '
        - '    '
        - '   9'
        - '    '
#  
# Проверка "кликабельное сообщение".
# Подробнее: http://docs.baronessdev.ru/BaronessAuth/configuration/antibot/click
click:
  # Текст с призывом нажать.
  text: '§6Нажмите сюда, чтобы подтвердить регистрацию.'
  # Текст, показывающийся при наведении курсора.
  hover: '§6§lДа-да, сюда!'
```
</details>

